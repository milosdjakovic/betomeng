---
---

<script>
  import type { Language } from "../bin/types";
  import { getLanguage } from "../bin/language";

  const language: Language = getLanguage() ?? "en";
  const lastVisitedPath: string =
    window.localStorage.getItem("last-visited-path") ?? "/";

  location.href = `${language}${lastVisitedPath}`;
</script>
